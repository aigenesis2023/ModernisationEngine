import type { CourseIR, ImageManifest, EvolutionConfig } from '../ir/types';

// Stub — Chunk 5 implements this fully
export async function processImages(course: CourseIR, config: EvolutionConfig): Promise<ImageManifest> {
  throw new Error('Image analyzer not yet implemented — run Chunk 5');
}
