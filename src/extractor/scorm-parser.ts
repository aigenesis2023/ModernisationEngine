import type { CourseIR } from '../ir/types';

// Stub — Chunk 3 implements this fully
export async function extractCourse(scormDir: string, verbose: boolean): Promise<CourseIR> {
  throw new Error('SCORM extractor not yet implemented — run Chunk 3');
}
