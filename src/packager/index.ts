import type { EvolutionConfig } from '../ir/types';

// Stub — Chunk 7 implements this fully
export async function packageScorm(config: EvolutionConfig): Promise<string> {
  throw new Error('Packager not yet implemented — run Chunk 7');
}
