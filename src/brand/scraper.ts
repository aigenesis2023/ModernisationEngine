import type { BrandProfile } from '../ir/types';

// Stub — Chunk 4 implements this fully
export async function scrapeBrand(url: string, verbose: boolean): Promise<BrandProfile> {
  throw new Error('Brand scraper not yet implemented — run Chunk 4');
}
